CREATE TABLE ACCOUNT (ID NUMBER(19) NOT NULL, AMOUNT NUMBER(10,5), CLIENT_ID NUMBER(19), PRIMARY KEY (ID))
CREATE TABLE CLIENT (ID NUMBER(19) NOT NULL, PRIMARY KEY (ID))
ALTER TABLE ACCOUNT ADD CONSTRAINT FK_ACCOUNT_CLIENT_ID FOREIGN KEY (CLIENT_ID) REFERENCES CLIENT (ID)
CREATE TABLE SEQUENCE (SEQ_NAME VARCHAR(50) NOT NULL, SEQ_COUNT NUMBER(19), PRIMARY KEY (SEQ_NAME))
INSERT INTO SEQUENCE(SEQ_NAME, SEQ_COUNT) values ('SEQ_GEN', 0)
